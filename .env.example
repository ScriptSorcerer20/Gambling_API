# Generate a value with: node -e "console.log(require('node:crypto').randomBytes(32).toString('hex'))"
TOKEN_SECRET=replace-with-a-generated-secret-at-least-32-characters
HOST=127.0.0.1
PORT=42069
# DATABASE_PATH=users.sqlite
# PUBLIC_ORIGIN=https://poker.example.com
# Comma-separated IPs/subnets of proxies you control; unset means no trust.
# TRUST_PROXY=loopback
